global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  # PushGateway - recebe métricas do coletor
  - job_name: 'pushgateway'
    static_configs:
      - targets: ['pushgateway:9091']
    honor_labels: true
